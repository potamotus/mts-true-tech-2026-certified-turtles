services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      MWS_API_KEY: ${MWS_API_KEY:-}
      MWS_API_BASE: ${MWS_API_BASE:-https://api.gpt.mws.ru}
    restart: unless-stopped
